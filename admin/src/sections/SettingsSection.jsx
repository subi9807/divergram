export default function SettingsSection({ adminKey, setAdminKey, loading, refresh, seedBulk, checkUserAdminAuth, authCheck }) {
  return <div className="card"><h2>설정/점검</h2><form className="row settings-form" onSubmit={(e)=>{e.preventDefault();refresh();}} style={{ marginBottom:12 }}><input type="text" autoComplete="username" value="admin" readOnly style={{ display:'none' }} /><input type="password" autoComplete="current-password" value={adminKey} onChange={(e)=>setAdminKey(e.target.value)} placeholder="ADMIN_API_KEY" /><button type="submit" disabled={loading}>{loading?'로딩...':'연결 저장'}</button><button type="button" onClick={seedBulk} disabled={loading}>대량 샘플 생성</button></form><p>사용자/관리자 로그인 체크를 실행할 수 있어.</p><div className="row settings-actions"><button onClick={checkUserAdminAuth}>사용자+관리자 로그인 체크</button></div>{authCheck&&<p style={{ marginTop:8 }}>관리자 로그인: <strong>{authCheck.adminOk?'정상':'실패'}</strong> / 일반 사용자 로그인: <strong>{authCheck.userOk?'정상':'실패'}</strong></p>}</div>;
}
