export default function TablesSection({ tables, selectedTable, setSelectedTable, refreshTableRows, tableRows }) {
  return <div className="card"><h2>테이블 조회</h2><div className="row" style={{ marginBottom:12 }}><select value={selectedTable} onChange={(e)=>setSelectedTable(e.target.value)}>{tables.map((t)=><option key={t.table} value={t.table}>{t.table} ({t.count})</option>)}</select><button onClick={()=>refreshTableRows(selectedTable)}>조회</button></div><table><thead><tr><th>table</th><th>count</th></tr></thead><tbody>{tables.map((t)=><tr key={t.table}><td>{t.table}</td><td>{t.count}</td></tr>)}</tbody></table><h3 style={{ marginTop:16 }}>선택 테이블 샘플(최대 100건)</h3><pre style={{ maxHeight:340,overflow:'auto',background:'#0b1220',color:'#dbeafe',padding:12,borderRadius:8 }}>{JSON.stringify(tableRows,null,2)}</pre></div>;
}
