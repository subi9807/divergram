export default function FeedsSection({ feedRows, feedPageRows, feedPage, feedTotalPages, setFeedPage }) {
  return <div className="card"><h2>피드 관리</h2><p style={{ marginTop:0,color:'#6b7280' }}>이미지 기반 피드 게시물 목록 ({feedRows.length}건)</p><table><thead><tr><th>THUMB</th><th>ID</th><th>USER</th><th>CAPTION</th><th>LOCATION</th><th>CREATED</th></tr></thead><tbody>{feedPageRows.map((p)=><tr key={p.id}><td>{p.image_url?<img src={p.image_url} alt="thumb" style={{ width:48,height:48,objectFit:'cover',borderRadius:8 }} />:<span style={{ color:'#9ca3af' }}>-</span>}</td><td>{p.id}</td><td>{p.user_id}</td><td>{p.caption||'-'}</td><td>{p.location||'-'}</td><td>{p.created_at?new Date(p.created_at).toLocaleString():'-'}</td></tr>)}</tbody></table><div className="row" style={{ marginTop:12,justifyContent:'space-between' }}><span style={{ color:'#6b7280',fontSize:13 }}>페이지 {feedPage} / {feedTotalPages}</span><div className="row"><button onClick={()=>setFeedPage((p)=>Math.max(1,p-1))} disabled={feedPage<=1}>이전</button><button onClick={()=>setFeedPage((p)=>Math.min(feedTotalPages,p+1))} disabled={feedPage>=feedTotalPages}>다음</button></div></div></div>;
}
