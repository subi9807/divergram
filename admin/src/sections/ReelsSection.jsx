export default function ReelsSection({ reelRows, reelPageRows, reelPage, reelTotalPages, setReelPage, getReelThumb }) {
  return <div className="card"><h2>릴스 관리</h2><p style={{ marginTop:0,color:'#6b7280' }}>비디오 기반 릴스 게시물 목록 ({reelRows.length}건)</p><table><thead><tr><th>THUMB</th><th>ID</th><th>USER</th><th>CAPTION</th><th>VIDEO URL</th><th>CREATED</th></tr></thead><tbody>{reelPageRows.map((p)=><tr key={p.id}><td>{getReelThumb(p)?<img src={getReelThumb(p)} alt="thumb" style={{ width:48,height:48,objectFit:'cover',borderRadius:8 }} />:<span style={{ color:'#9ca3af' }}>-</span>}</td><td>{p.id}</td><td>{p.user_id}</td><td>{p.caption||'-'}</td><td style={{ maxWidth:300,wordBreak:'break-all' }}>{p.video_url||'-'}</td><td>{p.created_at?new Date(p.created_at).toLocaleString():'-'}</td></tr>)}</tbody></table><div className="row" style={{ marginTop:12,justifyContent:'space-between' }}><span style={{ color:'#6b7280',fontSize:13 }}>페이지 {reelPage} / {reelTotalPages}</span><div className="row"><button onClick={()=>setReelPage((p)=>Math.max(1,p-1))} disabled={reelPage<=1}>이전</button><button onClick={()=>setReelPage((p)=>Math.min(reelTotalPages,p+1))} disabled={reelPage>=reelTotalPages}>다음</button></div></div></div>;
}
